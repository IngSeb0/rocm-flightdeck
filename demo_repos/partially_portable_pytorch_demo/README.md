# Partially Portable PyTorch Demo

This controlled fixture already uses a device abstraction and a neutral Python base
image, but it still includes one risky dependency and lacks ROCm migration artifacts.
